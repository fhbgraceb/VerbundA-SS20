package zweiteHausuebung.Schleifen;

import java.util.Scanner;

public class c_Ziffernsumme {

	public static void main(String[] args) {
		Scanner s = new Scanner(System.in);
		
		int number;
		int ziffer;
		int reste;
		int ziffernsumme;
		
		System.out.print("Bitte eine Zahl eingeben: ");
		number = s.nextInt();
		/*
		 * Damit das Programm auch f�r negative Zahlen geht,
		 * wird mit dem Absolutbetrag ("schneidet" das negative
		 * Vorzeichen ab) gerechnet.
		 */
		reste = Math.abs(number);

		/*
		 * Von der Zahl wird immer die letzte Ziffer "abgeschnitten"
		 * und zur Ziffernsumme addiert, bis die Restzahl "weg" (also 0) ist.
		 */
		
		ziffernsumme = 0;
		while (reste > 0)
		{
			ziffer = reste % 10;				// letzte Ziffer holen
			ziffernsumme += ziffer;				// zur Ziffernsumme addieren
			reste = (reste - ziffer) / 10;	// letzte Ziffer abschneiden

// Kontrollausgabe
//			System.out.println("Ziffer: " + ziffer + " Summe: " + ziffernsumme + " Restzahl: " + restZahl);
		}
		
		if (ziffernsumme % 3 == 0)
		{
			System.out.println(number + " ist durch 3 teilbar.");
		}
		else
		{
			System.out.println(number + " ist nicht durch 3 teilbar.");
		}

	}

}
